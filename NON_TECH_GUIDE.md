# 🚀 Non-Technical Guide: Task Manager Deployment (সহজ গাইড)

এই গাইডটি এমনভাবে তৈরি করা হয়েছে যাতে কোনো টেকনিক্যাল জ্ঞান ছাড়াই আপনি এই প্রজেক্টটি **Vercel**-এ ডিপ্লয় (লাইভ) করতে পারেন।

---

## ✅ ধাপ ১: প্রয়োজনীয় অ্যাকাউন্ট খোলা (Prerequisites)

প্রথমে নিচের সাইটগুলোতে অ্যাকাউন্ট খুলে নিন (সবগুলো ফ্রি):

1.  **GitHub** (কোড রাখার জন্য): [github.com](https://github.com)
2.  **Vercel** (ওয়েবসাইট লাইভ করার জন্য): [vercel.com](https://vercel.com)
3.  **Neon** (ডাটাবেস বা তথ্যের ভাণ্ডারের জন্য): [neon.tech](https://neon.tech)
4.  **Stripe** (পেমেন্ট বা টাকা লেনদেনের জন্য): [stripe.com](https://stripe.com)

---

## 🗄️ ধাপ ২: ডাটাবেস তৈরি করা (Neon)

1.  [neon.tech](https://neon.tech)-এ যান এবং **Sign Up** করুন (Google দিয়ে লগইন করা সহজ)।
2.  **"New Project"**-এ ক্লিক করুন।
3.  প্রজেক্টের একটা নাম দিন (যেমন: `taskmanager`) এবং **Create Project**-এ ক্লিক করুন।
4.  আপনাকে সাথে সাথে একটি **Connection String** দেওয়া হবে।
    - দেখতে অনেকটা এমন: `postgres://neondb_owner:password@ep-something.aws.neon.tech/neondb?sslmode=require`
    - এটি কপি করে কোথাও সেভ করে রাখুন। এটিই আপনার `DATABASE_URL`।

---

## 💳 ধাপ ৩: পেমেন্ট সেটআপ (Stripe)

1.  Stripe-এ লগইন করে **Developers** ড্যাশবোর্ডে যান।
2.  **API keys** ট্যাবে যান।
3.  সেখান থেকে `Publishable key` (শুরু হয় `pk_test_` দিয়ে) এবং `Secret key` (শুরু হয় `sk_test_` দিয়ে) কপি করে রাখুন।

---

## 📤 ধাপ ৪: কোড গিটহাবে আপলোড করা (GitHub)

1.  আপনার কম্পিউটারে যেখানে কোড আছে, সেখানে টার্মিনাল ওপেন করুন।
2.  নিচের কমান্ডগুলো একে একে দিন (যদি আগে না দিয়ে থাকেন):

```bash
git add .
git commit -m "Ready for production"
```

3.  GitHub-এ গিয়ে একটি **New Repository** তৈরি করুন (নাম দিন `taskmanager`)।
4.  GitHub আপনাকে কিছু কমান্ড দেখাবে। সেখান থেকে নিচের মতো কমান্ডগুলো কপি করে আপনার টার্মিনালে পেস্ট করুন:

```bash
git remote add origin https://github.com/আপনার_ইউজারনেম/taskmanager.git
git branch -M main
git push -u origin main
```

---

## 🚀 ধাপ ৫: লাইভ করা (Vercel Deployment)

1.  Vercel ড্যাশবোর্ডে গিয়ে **"Add New..."** > **"Project"**-এ ক্লিক করুন।
2.  আপনার GitHub কানেক্ট করুন এবং `taskmanager` প্রজেক্টটি সিলেক্ট করুন।
3.  **Framework Preset** হিসেবে `Next.js` অটো সিলেক্ট হওয়ার কথা। না হলে `Next.js` সিলেক্ট করুন।
4.  **Environment Variables** সেকশনে ক্লিক করে নিচের তথ্যগুলো যোগ করুন:

| নাম (Name) | মান (Value) | বর্ণনা |
| :--- | :--- | :--- |
| `DATABASE_URL` | `postgresql://...` | ধাপ ২-এ পাওয়া ডাটাবেস লিংক |
| `JWT_SECRET` | `যেকোনো_গোপন_শব্দ` | যেমন: `mysecretkey123` |
| `STRIPE_SECRET_KEY` | `sk_test_...` | ধাপ ৩-এ পাওয়া সিক্রেট কি |
| `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` | `pk_test_...` | ধাপ ৩-এ পাওয়া পাবলিক কি |
| `NEXT_PUBLIC_API_URL` | `https://আপনার-সাইট.vercel.app/api` | (ডিপ্লয় হওয়ার পর এটি আপডেট করতে হতে পারে, প্রথমে খালি রাখতে পারেন বা `https://taskmanager.vercel.app/api` দিতে পারেন) |
| `ADMIN_SECRET_KEY` | `admin123` | অ্যাডমিন প্যানেলে ঢোকার গোপন চাবি |

5.  সবশেষে **"Deploy"** বাটনে ক্লিক করুন।
6.  ১-২ মিনিট অপেক্ষা করুন। কনফেটি (🎉) উড়লে বুঝবেন আপনার সাইট লাইভ হয়ে গেছে!

---

## ⚙️ ধাপ ৬: অ্যাডমিন ও শেষ কাজ

1.  ডিপ্লয় হয়ে গেলে Vercel আপনাকে একটি লিংক দেবে (যেমন: `https://taskmanager.vercel.app`)।
2.  **Environment Variables**-এ গিয়ে `NEXT_PUBLIC_API_URL` আপডেট করুন (যদি আগে না দিয়ে থাকেন) এবং `https://আপনার-ডোমেইন.vercel.app/api` বসিয়ে **Redeploy** করুন।
3.  **অ্যাডমিন প্যানেল**: `https://আপনার-ডোমেইন.vercel.app/admin` এ যান।

---

## 🛠️ রক্ষণাবেক্ষণ (Maintenance)

- **কোড আপডেট**: আপনি যখনই কম্পিউটারে কোড পরিবর্তন করে `git push` করবেন, Vercel অটোমেটিক আপনার সাইট আপডেট করে দেবে।
- **সমস্যা হলে**: Vercel-এর **Logs** ট্যাবে গিয়ে এরর দেখতে পারবেন।

কোনো সমস্যা হলে আমাকে (বা কোনো ডেভেলপারকে) এই গাইড দেখালে তারা সহজেই সাহায্য করতে পারবে। শুভকামনা! 🚀
